# HarnessAgent

基于 DeepSeek 的 coding agent 实验项目。

- s01: minimal agent loop with bash tool
- s02: 5 tools (bash / read_file / write_file / edit_file / glob)

运行前把 `.env.example` 复制为 `.env` 并填入 DEEPSEEK_API_KEY。
